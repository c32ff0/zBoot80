This is a very customisable bootloader for the
z80 with a small (optional) command line.

Just run 'make' and it should take care of everything else. 

(It is currently useless and an experiment for using git)
